on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    name: Testing my action
    steps:
      - name: Test steps
        id: test
        uses: ammar-ahmed22/compile-typst-action@v0.0.9
        with:
          source-files: main.typ