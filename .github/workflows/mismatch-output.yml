on: 
  workflow_dispatch:
name: Testing with Mismatched Number of Output Paths
jobs:
  test:
    runs-on: ubuntu-latest
    name: Testing Action
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test with mismatch output_paths
        uses: ammar-ahmed22/compile-typst-action@main
        with:
          source_paths: testing/file1.typ testing/file2.typ
          output_paths: myfile1.pdf
      # - name: Upload PDF
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: PDF
      #     path: "**/*.pdf"
      # - name: Release
      #   uses: softprops/actions-gh-release@v1
      #   with:
      #     name: "${{ github.ref_name }}"
      #     file: "output.pdf"