on: 
  workflow_dispatch:
  push:
    branches:
      - main
name: Testing with No Source Paths
jobs:
  test:
    runs-on: ubuntu-latest
    name: Testing with No Source Path
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test without source
        uses: ammar-ahmed22/compile-typst-action@main
        with:
          output_paths: myfile2.pdf
      # - name: Upload PDF
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: PDF
      #     path: "**/*.pdf"
      # - name: Release
      #   uses: softprops/actions-gh-release@v1
      #   with:
      #     name: "${{ github.ref_name }}"
      #     file: "output.pdf"